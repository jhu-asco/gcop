1.) if ./docs is empty, run command "doxygen"

2.) See docs/html/index.html

3.) If using Bullet make sure you compile bullet with Double Precision on since optimization loses precision if used with float

