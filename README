1.) if ./docs is empty, run command "doxygen"

2.) See docs/html/index.html

3.) If using Bullet make sure you compile bullet with Double Precision (USE_BULLET) on since optimization loses precision if used with float

Installation Procedure:
git clone https://git.lcsr.jhu.edu/mkobila1/gcop # Clone the directory
git submodule init #Initialize submodules
git submodule update #Clones bullet3 specific commit needed for this
cd bullet3/build3 #Go into bullet3 for building
cmake -DBUILD_SHARED_LIBS=ON -DUSE_DOUBLE_PRECISION=TRUE ..
cd .. & make build # Go back to gcop and create a build directory
cmake -DUSE_BULLET=ON .. & make -j `nproc`
